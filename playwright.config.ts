// Playwright config (TypeScript stub kept for tooling) — actual runtime config is in playwright.config.cjs
// This minimal export avoids importing `@playwright/test` during `next build` which may run without devDependencies.
export default {};
